name: ci
on:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: run echo
        run: echo hello world!
        
      - name: 构建项目
        run: go build ./...
        
      - name: 测试代码
        run: |
          cd hello/
          go test 
          
